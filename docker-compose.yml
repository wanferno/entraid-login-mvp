services:
  bff:
    build:
      context: ./server
    ports:
      - "3001:3001"
    env_file: .env
    restart: unless-stopped

  frontend:
    build: .
    ports:
      - "5173:80"
    depends_on:
      - bff
    restart: unless-stopped
