{
  "name": "entraid-login-mvp",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:bff": "concurrently -n bff,front -c cyan,magenta \"npm run bff\" \"npm run dev\"",
    "bff": "npm --prefix server run start",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@azure/msal-browser": "^4.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
